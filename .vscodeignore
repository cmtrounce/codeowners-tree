# Source files
src/**

# Test files
test/**

# Development files
.vscode/**
.git/**
.gitignore
tsconfig.json
.eslintrc.json

# Build artifacts (exclude source maps and type definitions, but keep compiled JS)
out/**/*.map
out/**/*.d.ts

# Documentation
CHANGELOG.md

# IDE files
.vscode-test/**
*.vsix

# Dependencies - exclude all node_modules by default
node_modules/**

# But include only the runtime dependencies we actually need
!node_modules/graphviz/**
!node_modules/which/**
!node_modules/@types/graphviz/**
